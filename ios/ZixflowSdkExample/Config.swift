import Foundation

/// Replace YOUR_API_KEY with a key from Zixflow → Settings → Developers → API Keys.
/// Do not commit real keys.
enum Config {
    static let apiKey = "YOUR_API_KEY"
    static let enableOptionalModules = true
}
